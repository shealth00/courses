# Copy this file to .env for local development.
# On Hostinger, set these as environment variables in your app's settings
# instead (Hostinger auto-populates them once the app is linked to Supabase,
# but confirm these values match your project).

SUPABASE_URL=https://txosnawkzmhkpzjzdgkn.supabase.co
SUPABASE_ANON_KEY=sb_publishable_LGTqfDN-Vc9qqmarZ1lp5g_TThtnHPf

# Service role key — server-side only, used by supabase/seed.js to write
# content past Row Level Security. Get it from Project Settings > API >
# service_role (secret). NEVER commit this or expose it to the browser.
SUPABASE_SERVICE_KEY=

PORT=3000
